# Copy to .env.local for local dev, fill in your Google Cloud OAuth Client ID.
# See https://console.cloud.google.com/ → APIs & Services → Credentials.
# Required for Google Drive / Sheets sync. Leave blank to disable that feature.
VITE_GOOGLE_CLIENT_ID=
